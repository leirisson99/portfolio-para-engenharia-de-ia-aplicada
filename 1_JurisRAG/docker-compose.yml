services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: jurisrag
      POSTGRES_PASSWORD: jurisrag
      POSTGRES_DB: jurisrag
    ports:
      - "5432:5432"
    volumes:
      - jurisrag_pgdata:/var/lib/postgresql/data

volumes:
  jurisrag_pgdata:
